#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 1 ]; then
    echo "Usage: scripts/run-gauntlet MODEL [ENDPOINT]"
    exit 1
fi

MODEL="$1"
ENDPOINT="${2:-http://127.0.0.1:8000}"

cd "$(dirname "$0")/.."

python3 -m benchmark.runner \
    --model "$MODEL" \
    --endpoint "$ENDPOINT"
